# LangChain-Go v0.5.1 用户指南

**Agent Skill 系统 - 可组合的智能体能力**

---

## 📖 目录

1. [概述](#概述)
2. [核心概念](#核心概念)
3. [快速开始](#快速开始)
4. [基础使用](#基础使用)
5. [高级功能](#高级功能)
6. [内置 Skills](#内置-skills)
7. [自定义 Skill](#自定义-skill)
8. [最佳实践](#最佳实践)
9. [API 参考](#api-参考)

---

## 概述

### 什么是 Agent Skill？

**Agent Skill** 是一个可加载的智能体能力模块，包含：
- 专业化的提示词和知识
- 特定领域的工具
- 执行逻辑和状态

Skill 系统采用**渐进式披露（Progressive Disclosure）**设计模式，按需加载专业能力，避免单一 Agent 过度臃肿。

### 为什么需要 Skill？

**问题**:
- ❌ 单一 Agent 承担所有任务，提示词臃肿
- ❌ 不同场景需要不同专业能力
- ❌ 团队协作困难，难以独立开发和维护

**解决方案**:
- ✅ 按需加载专业能力
- ✅ 模块化、可组合、可复用
- ✅ 支持团队独立开发
- ✅ 动态切换能力

### 核心特性

1. **统一接口** - 所有 Skill 实现相同接口
2. **生命周期管理** - Load/Unload 完整生命周期
3. **依赖管理** - 自动解析和加载依赖
4. **动态工具注册** - Skill 可以注册/卸载工具
5. **分类和标签** - 按分类或标签查找 Skill
6. **并发安全** - 线程安全设计

---

## 核心概念

### Skill 接口

```go
type Skill interface {
    // 标识和元数据
    ID() string
    Name() string
    Description() string
    Category() SkillCategory
    Tags() []string
    
    // 生命周期
    Load(ctx context.Context, config *LoadConfig) error
    Unload(ctx context.Context) error
    IsLoaded() bool
    
    // 能力提供
    GetTools() []tools.Tool
    GetSystemPrompt() string
    GetExamples() []SkillExample
    GetMetadata() *SkillMetadata
    
    // 依赖管理
    Dependencies() []string
}
```

### Skill 分类

| 分类 | 说明 | 示例 |
|------|------|------|
| `CategoryCoding` | 编程相关 | 代码编写、调试、重构 |
| `CategoryDataAnalysis` | 数据分析 | 统计分析、可视化 |
| `CategoryKnowledge` | 知识问答 | 百科知识、事实查询 |
| `CategoryCreative` | 创意写作 | 文案、故事、诗歌 |
| `CategoryResearch` | 研究调研 | 文献综述、调研报告 |
| `CategoryAutomation` | 自动化 | 流程自动化、脚本 |
| `CategoryCommunication` | 沟通 | 邮件、客服、翻译 |
| `CategoryGeneral` | 通用 | 通用能力 |

### 生命周期

```
注册 → 加载 → 使用 → 卸载 → 注销
  ↓      ↓      ↓      ↓      ↓
Register → Load → Use → Unload → Unregister
```

---

## 快速开始

### 安装

确保你已经安装了 LangChain-Go v0.5.1+：

```bash
go get github.com/zhucl121/langchain-go@v0.5.1
```

### 5 分钟上手

```go
package main

import (
    "context"
    "fmt"
    "log"
    
    "github.com/zhucl121/langchain-go/pkg/skills"
    "github.com/zhucl121/langchain-go/pkg/skills/builtin"
    "github.com/zhucl121/langchain-go/core/agents"
    "github.com/zhucl121/langchain-go/core/chat"
)

func main() {
    ctx := context.Background()
    
    // 1. 创建 Skill 管理器
    skillManager := skills.NewSkillManager()
    
    // 2. 注册 Skill
    codingSkill := builtin.NewCodingSkill()
    skillManager.Register(codingSkill)
    
    // 3. 加载 Skill
    config := &skills.LoadConfig{
        AutoLoadDependencies: true,
    }
    skillManager.Load(ctx, "coding", config)
    
    // 4. 创建 Agent（带 Skill）
    chatModel := chat.NewOpenAIChatModel(/* config */)
    executor := agents.NewAgentExecutor(agents.AgentConfig{
        Type:          agents.AgentTypeReAct,
        LLM:           chatModel,
        SkillManager:  skillManager,
        EnabledSkills: []string{"coding"},
    })
    
    // 5. 执行任务
    result, _ := executor.Run(ctx, "写一个快速排序算法")
    fmt.Println(result.Output)
}
```

---

## 基础使用

### 创建 Skill Manager

```go
import "github.com/zhucl121/langchain-go/pkg/skills"

// 创建管理器
manager := skills.NewSkillManager()
```

### 注册 Skill

```go
// 使用内置 Skill
codingSkill := builtin.NewCodingSkill()
if err := manager.Register(codingSkill); err != nil {
    log.Fatal(err)
}

// 或创建自定义 Skill
customSkill := skills.NewBaseSkill(
    skills.WithID("my-skill"),
    skills.WithName("我的技能"),
    skills.WithCategory(skills.CategoryGeneral),
)
manager.Register(customSkill)
```

### 加载 Skill

```go
ctx := context.Background()

// 基础加载
config := &skills.LoadConfig{}
err := manager.Load(ctx, "coding", config)

// 自动加载依赖
config = &skills.LoadConfig{
    AutoLoadDependencies: true,
}
err = manager.LoadWithDependencies(ctx, "coding", config)
```

### 卸载 Skill

```go
// 卸载单个 Skill
err := manager.Unload(ctx, "coding")

// 注销 Skill
err = manager.Unregister("coding")
```

### 查询 Skill

```go
// 获取单个 Skill
skill, err := manager.Get("coding")

// 列出所有 Skill
allSkills := manager.List()

// 列出已加载的 Skill
loadedSkills := manager.ListLoaded()

// 按分类查找
codingSkills := manager.FindByCategory(skills.CategoryCoding)

// 按标签查找
pythonSkills := manager.FindByTags([]string{"python", "coding"})
```

### 集成到 Agent

```go
// 创建 Agent 时指定 Skill
executor := agents.NewAgentExecutor(agents.AgentConfig{
    Type:          agents.AgentTypeReAct,
    LLM:           chatModel,
    Tools:         basicTools,      // 基础工具
    SkillManager:  skillManager,    // Skill 管理器
    EnabledSkills: []string{         // 启用的 Skill
        "coding",
        "data-analysis",
    },
})

// Agent 会自动：
// 1. 加载指定的 Skill
// 2. 聚合 Skill 提供的工具
// 3. 组合 Skill 的系统提示词
```

---

## 高级功能

### 依赖管理

```go
// 定义有依赖的 Skill
advancedSkill := skills.NewBaseSkill(
    skills.WithID("advanced-coding"),
    skills.WithName("高级编程"),
    skills.WithDependencies("coding", "data-analysis"), // 依赖其他 Skill
)

// 加载时自动解析依赖
manager.Register(advancedSkill)
manager.LoadWithDependencies(ctx, "advanced-coding", config)
// 会自动先加载 "coding" 和 "data-analysis"
```

### 动态工具管理

```go
// Skill 可以动态添加工具
skill := manager.Get("coding")
if baseSkill, ok := skill.(*skills.BaseSkill); ok {
    // 添加工具
    baseSkill.AddTool(myNewTool)
    
    // 移除工具
    baseSkill.RemoveTool("old-tool-name")
}
```

### 自定义加载逻辑

```go
skill := skills.NewBaseSkill(
    skills.WithID("custom"),
    skills.WithName("自定义"),
    
    // 自定义加载钩子
    skills.WithLoadHook(func(ctx context.Context, config *LoadConfig) error {
        // 初始化资源
        log.Println("Loading custom skill...")
        
        // 连接数据库、加载配置等
        if err := initializeResources(); err != nil {
            return err
        }
        
        return nil
    }),
    
    // 自定义卸载钩子
    skills.WithUnloadHook(func(ctx context.Context) error {
        // 清理资源
        log.Println("Unloading custom skill...")
        return cleanupResources()
    }),
)
```

### 循环依赖检测

Skill Manager 会自动检测循环依赖：

```go
// 这会导致错误
skillA := skills.NewBaseSkill(
    skills.WithID("skill-a"),
    skills.WithDependencies("skill-b"),
)

skillB := skills.NewBaseSkill(
    skills.WithID("skill-b"),
    skills.WithDependencies("skill-a"), // 循环依赖！
)

manager.Register(skillA)
manager.Register(skillB)

// 加载时会返回 ErrCircularDependency
err := manager.LoadWithDependencies(ctx, "skill-a", config)
// err == skills.ErrCircularDependency
```

---

## 内置 Skills

### 1. Coding Skill

**ID**: `coding`  
**分类**: `CategoryCoding`  
**描述**: 提供代码编写、调试和重构能力

```go
skill := builtin.NewCodingSkill()
```

**特性**:
- 编写高质量代码
- 代码审查和重构建议
- 调试和问题诊断
- 性能优化建议
- 单元测试编写

**适用场景**:
- 代码助手
- 编程教育
- 代码审查工具

### 2. Data Analysis Skill

**ID**: `data-analysis`  
**分类**: `CategoryDataAnalysis`  
**描述**: 提供数据探索、统计分析和可视化建议

```go
skill := builtin.NewDataAnalysisSkill()
```

**特性**:
- 数据探索和清洗
- 统计分析和假设检验
- 数据可视化
- 趋势分析和预测
- 报告撰写

**适用场景**:
- 数据分析助手
- BI 报告生成
- 数据科学工作流

### 3. Knowledge Query Skill

**ID**: `knowledge-query`  
**分类**: `CategoryKnowledge`  
**描述**: 提供准确、全面的知识问答

```go
skill := builtin.NewKnowledgeQuerySkill()
```

**特性**:
- 回答各领域知识问题
- 提供准确的事实和数据
- 引用可靠信息来源
- 解释复杂概念
- 多角度分析

**适用场景**:
- 知识问答系统
- 客服机器人
- 教育辅助

### 4. Research Skill

**ID**: `research`  
**分类**: `CategoryResearch`  
**描述**: 提供深度调研和分析能力

```go
skill := builtin.NewResearchSkill()
```

**特性**:
- 文献调研和综述
- 竞品分析
- 市场调研
- 技术评估
- 研究报告撰写

**适用场景**:
- 调研助手
- 市场分析
- 技术选型

---

## 自定义 Skill

### 基础自定义

```go
package myskills

import (
    "context"
    "github.com/zhucl121/langchain-go/pkg/skills"
    "github.com/zhucl121/langchain-go/core/tools"
)

// 创建自定义 Skill
func NewTranslationSkill() skills.Skill {
    return skills.NewBaseSkill(
        skills.WithID("translation"),
        skills.WithName("翻译助手"),
        skills.WithDescription("提供多语言翻译能力"),
        skills.WithCategory(skills.CategoryCommunication),
        skills.WithTags("translation", "language", "i18n"),
        
        // 系统提示词
        skills.WithSystemPrompt(`你是一个专业的翻译助手，擅长：
- 准确翻译多种语言
- 保持原文风格和语气
- 处理专业术语
- 提供多种翻译选项

翻译原则：
1. 准确性优先
2. 保持原文风格
3. 注意文化差异
4. 标注不确定的翻译`),
        
        // 工具
        skills.WithTools(
            tools.NewGoogleTranslateTool(),
            tools.NewDeepLTool(),
        ),
        
        // Few-shot 示例
        skills.WithExamples(
            skills.SkillExample{
                Input:  "Translate 'Hello World' to Chinese",
                Output: "你好，世界",
                Reasoning: "直译，保持简洁友好的语气",
            },
        ),
    )
}
```

### 高级自定义（实现 Skill 接口）

```go
package myskills

import (
    "context"
    "sync"
    
    "github.com/zhucl121/langchain-go/pkg/skills"
    "github.com/zhucl121/langchain-go/core/tools"
)

// CustomSkill 自定义 Skill
type CustomSkill struct {
    id          string
    name        string
    tools       []tools.Tool
    loaded      bool
    mu          sync.RWMutex
    
    // 自定义字段
    apiClient   *MyAPIClient
    cache       *Cache
}

// NewCustomSkill 创建自定义 Skill
func NewCustomSkill(apiKey string) *CustomSkill {
    return &CustomSkill{
        id:   "custom-skill",
        name: "自定义技能",
        apiClient: NewMyAPIClient(apiKey),
        cache: NewCache(),
    }
}

// ID 实现 Skill 接口
func (s *CustomSkill) ID() string { return s.id }

// Name 实现 Skill 接口
func (s *CustomSkill) Name() string { return s.name }

// Load 实现 Skill 接口
func (s *CustomSkill) Load(ctx context.Context, config *skills.LoadConfig) error {
    s.mu.Lock()
    defer s.mu.Unlock()
    
    if s.loaded {
        return skills.ErrSkillAlreadyLoaded
    }
    
    // 自定义加载逻辑
    if err := s.apiClient.Connect(); err != nil {
        return err
    }
    
    // 初始化工具
    s.tools = []tools.Tool{
        NewCustomTool(s.apiClient),
    }
    
    s.loaded = true
    return nil
}

// Unload 实现 Skill 接口
func (s *CustomSkill) Unload(ctx context.Context) error {
    s.mu.Lock()
    defer s.mu.Unlock()
    
    if !s.loaded {
        return skills.ErrSkillNotLoaded
    }
    
    // 清理资源
    s.apiClient.Disconnect()
    s.cache.Clear()
    s.tools = nil
    
    s.loaded = false
    return nil
}

// GetTools 实现 Skill 接口
func (s *CustomSkill) GetTools() []tools.Tool {
    s.mu.RLock()
    defer s.mu.RUnlock()
    return s.tools
}

// ... 实现其他接口方法 ...
```

---

## 最佳实践

### 1. Skill 粒度

✅ **推荐**：单一职责，专注特定领域

```go
// Good: 专注编程
codingSkill := builtin.NewCodingSkill()

// Good: 专注数据分析
dataSkill := builtin.NewDataAnalysisSkill()
```

❌ **不推荐**：一个 Skill 包含所有能力

```go
// Bad: 太宽泛
superSkill := NewEverythingSkill() // 包含编程、数据分析、翻译等所有功能
```

### 2. 按需加载

✅ **推荐**：根据任务类型动态加载

```go
// 任务分类
if taskType == "coding" {
    manager.Load(ctx, "coding", config)
} else if taskType == "analysis" {
    manager.Load(ctx, "data-analysis", config)
}

// 执行完后卸载
defer manager.Unload(ctx, skillID)
```

❌ **不推荐**：一次性加载所有 Skill

```go
// Bad: 浪费资源
manager.Load(ctx, "coding", config)
manager.Load(ctx, "data-analysis", config)
manager.Load(ctx, "research", config)
// ... 加载所有 Skill
```

### 3. 资源管理

✅ **推荐**：在 Load/Unload 中管理资源

```go
skill := skills.NewBaseSkill(
    skills.WithLoadHook(func(ctx context.Context, config *LoadConfig) error {
        // 初始化资源
        return connectDatabase()
    }),
    skills.WithUnloadHook(func(ctx context.Context) error {
        // 清理资源
        return disconnectDatabase()
    }),
)
```

✅ **推荐**：使用 defer 确保卸载

```go
if err := manager.Load(ctx, "coding", config); err != nil {
    return err
}
defer manager.Unload(ctx, "coding")

// 使用 Skill
result, _ := executor.Run(ctx, task)
```

### 4. 错误处理

✅ **推荐**：检查错误并优雅降级

```go
// 尝试加载 Skill
if err := manager.Load(ctx, "advanced-skill", config); err != nil {
    // 降级到基础 Skill
    log.Printf("Failed to load advanced skill: %v, using basic skill", err)
    manager.Load(ctx, "basic-skill", config)
}
```

### 5. 并发安全

✅ **推荐**：使用 Manager 管理并发访问

```go
// Manager 已经是并发安全的
var wg sync.WaitGroup
for _, skillID := range skillIDs {
    wg.Add(1)
    go func(id string) {
        defer wg.Done()
        manager.Load(ctx, id, config) // 安全
    }(skillID)
}
wg.Wait()
```

### 6. 组合模式

✅ **推荐**：组合多个小 Skill 而非一个大 Skill

```go
// Good: 组合
manager.Register(builtin.NewCodingSkill())
manager.Register(builtin.NewDataAnalysisSkill())
manager.Register(builtin.NewResearchSkill())

// 根据需要加载
manager.Load(ctx, "coding", config)
manager.Load(ctx, "research", config)
```

---

## API 参考

### Skill 接口

```go
type Skill interface {
    ID() string
    Name() string
    Description() string
    Category() SkillCategory
    Tags() []string
    Load(ctx context.Context, config *LoadConfig) error
    Unload(ctx context.Context) error
    IsLoaded() bool
    GetTools() []tools.Tool
    GetSystemPrompt() string
    GetExamples() []SkillExample
    GetMetadata() *SkillMetadata
    Dependencies() []string
}
```

### SkillManager 接口

```go
type SkillManager interface {
    Register(skill Skill) error
    Unregister(skillID string) error
    Load(ctx context.Context, skillID string, config *LoadConfig) error
    Unload(ctx context.Context, skillID string) error
    Get(skillID string) (Skill, error)
    List() []Skill
    ListLoaded() []Skill
    FindByCategory(category SkillCategory) []Skill
    FindByTags(tags []string) []Skill
    LoadWithDependencies(ctx context.Context, skillID string, config *LoadConfig) error
}
```

### 错误类型

```go
var (
    ErrSkillNotFound          = errors.New("skill not found")
    ErrSkillAlreadyLoaded     = errors.New("skill already loaded")
    ErrSkillNotLoaded         = errors.New("skill not loaded")
    ErrSkillAlreadyRegistered = errors.New("skill already registered")
    ErrCircularDependency     = errors.New("circular dependency detected")
    ErrDependencyNotMet       = errors.New("dependency not met")
    ErrInvalidSkillConfig     = errors.New("invalid skill config")
)
```

---

## 常见问题

### Q1: Skill 和 Tool 有什么区别？

**A**: 
- **Tool** 是单一的、原子的功能（如"搜索网页"、"计算器"）
- **Skill** 是能力模块，包含多个 Tool + 提示词 + 示例（如"编程助手" Skill 包含代码分析工具、格式化工具等）

### Q2: 什么时候应该使用 Skill？

**A**: 当满足以下条件之一时考虑使用 Skill：
- 需要特定领域的专业能力（编程、数据分析等）
- 需要组合多个工具和提示词
- 需要按需加载能力（避免臃肿）
- 需要团队协作（不同团队开发不同 Skill）

### Q3: Skill 会影响性能吗？

**A**: 
- **未加载时**：零性能开销
- **加载时**：< 10ms（大部分情况）
- **使用时**：工具查找 < 1ms

### Q4: 可以在运行时切换 Skill 吗？

**A**: 可以！这是 Skill 系统的核心优势之一：

```go
// 加载编程 Skill
manager.Load(ctx, "coding", config)
result := executor.Run(ctx, "写代码...")

// 切换到数据分析 Skill
manager.Unload(ctx, "coding")
manager.Load(ctx, "data-analysis", config)
result = executor.Run(ctx, "分析数据...")
```

### Q5: 如何共享 Skill？

**A**: 将 Skill 打包为独立的 Go 包：

```go
// github.com/yourorg/custom-skills
package customskills

func NewYourSkill() skills.Skill {
    return skills.NewBaseSkill(...)
}
```

然后在其他项目中引入：

```go
import "github.com/yourorg/custom-skills"

manager.Register(customskills.NewYourSkill())
```

---

## 更多资源

- [实施计划](V0.5.1_IMPLEMENTATION_PLAN.md)
- [完成报告](V0.5.1_COMPLETION_REPORT.md)
- [示例程序](../examples/)
- [API 文档](https://pkg.go.dev/github.com/zhucl121/langchain-go/pkg/skills)

---

**版本**: v0.5.1  
**最后更新**: 2026-01-23  
**维护者**: LangChain-Go Team
