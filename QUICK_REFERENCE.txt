┏━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
┃                                                                      ┃
┃               🧪 LangChain-Go 测试环境快速参考                      ┃
┃                                                                      ┃
┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛

╔════════════════════════════════════════════════════════════════════╗
║ 🚀 快速开始                                                        ║
╚════════════════════════════════════════════════════════════════════╝

  # 方式 1: 一键演示 (首次推荐)
  bash scripts/demo.sh

  # 方式 2: Make 命令 (日常推荐)
  make -f Makefile.test test-env-up    # 启动
  make -f Makefile.test test           # 测试
  make -f Makefile.test test-env-down  # 停止

  # 方式 3: 一键测试
  bash scripts/quick-start-test.sh

╔════════════════════════════════════════════════════════════════════╗
║ 📋 服务信息                                                        ║
╚════════════════════════════════════════════════════════════════════╝

  服务          地址                 凭证
  ────────────  ──────────────────  ────────────────
  Redis         localhost:6379      密码: redis123
  Milvus        localhost:19530     无
  Milvus HTTP   localhost:9091      无

╔════════════════════════════════════════════════════════════════════╗
║ 🛠️ 常用命令                                                       ║
╚════════════════════════════════════════════════════════════════════╝

  环境管理
  ────────────────────────────────────────────────────────────────
  make -f Makefile.test test-env-up      # 启动环境
  make -f Makefile.test test-env-down    # 停止环境
  make -f Makefile.test test-env-status  # 查看状态

  运行测试
  ────────────────────────────────────────────────────────────────
  make -f Makefile.test test             # 所有测试
  make -f Makefile.test test-redis       # Redis 测试
  make -f Makefile.test test-milvus      # Milvus 测试
  make -f Makefile.test test-coverage    # 覆盖率报告

  特定测试
  ────────────────────────────────────────────────────────────────
  go test ./core/cache -v -run Redis
  go test ./retrieval/vectorstores -v -run Milvus
  go test ./core/cache -v -run TestRedisCache_Set

  查看日志
  ────────────────────────────────────────────────────────────────
  docker compose -f docker-compose.test.yml logs -f
  docker compose -f docker-compose.test.yml logs -f redis
  docker compose -f docker-compose.test.yml logs -f milvus

╔════════════════════════════════════════════════════════════════════╗
║ 🔍 验证和调试                                                      ║
╚════════════════════════════════════════════════════════════════════╝

  验证环境
  ────────────────────────────────────────────────────────────────
  bash scripts/verify-setup.sh

  测试连接
  ────────────────────────────────────────────────────────────────
  docker exec langchain-go-redis redis-cli -a redis123 ping
  curl http://localhost:9091/healthz

  进入容器
  ────────────────────────────────────────────────────────────────
  docker exec -it langchain-go-redis redis-cli -a redis123
  docker exec -it langchain-go-milvus bash

  查看资源
  ────────────────────────────────────────────────────────────────
  docker stats --filter "name=langchain-go-"
  docker ps --filter "name=langchain-go-"

╔════════════════════════════════════════════════════════════════════╗
║ 🧹 清理                                                            ║
╚════════════════════════════════════════════════════════════════════╝

  # 停止但保留数据
  docker compose -f docker-compose.test.yml down

  # 停止并删除数据
  docker compose -f docker-compose.test.yml down -v

  # 完全清理
  docker compose -f docker-compose.test.yml down -v --rmi all

╔════════════════════════════════════════════════════════════════════╗
║ 📚 文档                                                            ║
╚════════════════════════════════════════════════════════════════════╝

  文件名                    说明                 阅读时间
  ────────────────────────  ───────────────────  ────────
  INSTALLATION_COMPLETE.md  安装完成总结         5 分钟
  QUICK_TEST_START.md       快速开始指南 ⭐      5 分钟
  TEST_GUIDE.md             详细测试指南         15 分钟
  TEST_SETUP_SUMMARY.md     配置总结             3 分钟
  TEST_ARCHITECTURE.txt     架构图               5 分钟
  TEST_README.md            简要说明             2 分钟

╔════════════════════════════════════════════════════════════════════╗
║ ❓ 常见问题                                                        ║
╚════════════════════════════════════════════════════════════════════╝

  Q: Docker 未运行？
  A: 启动 Docker Desktop

  Q: 端口被占用？
  A: docker compose -f docker-compose.test.yml down

  Q: Milvus 启动慢？
  A: 首次启动需 1-2 分钟，请耐心等待

  Q: 测试被跳过？
  A: 启动测试环境后再运行测试

╔════════════════════════════════════════════════════════════════════╗
║ 🎯 最佳实践                                                        ║
╚════════════════════════════════════════════════════════════════════╝

  日常开发流程
  ────────────────────────────────────────────────────────────────
  1. 早上: make -f Makefile.test test-env-up
  2. 开发: make -f Makefile.test test (频繁运行)
  3. 下班: make -f Makefile.test test-env-down

  提交前检查
  ────────────────────────────────────────────────────────────────
  1. make -f Makefile.test test
  2. make -f Makefile.test test-coverage
  3. go vet ./...

╔════════════════════════════════════════════════════════════════════╗
║ 🆘 获取帮助                                                        ║
╚════════════════════════════════════════════════════════════════════╝

  make -f Makefile.test help        # 查看所有命令
  bash scripts/demo.sh              # 运行交互式演示
  cat QUICK_TEST_START.md           # 阅读快速指南

┏━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
┃                         祝测试顺利！🚀                              ┃
┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛
